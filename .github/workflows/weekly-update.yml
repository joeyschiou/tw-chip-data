name: weekly-update

# 週更資料(集保 holders / 流通 float / 月營收 revenue)。
# 集保 0c 實測:每週快照(週四/五結算),FinMind 端延遲數日釋出。
# 主排:UTC 週五 23:00 = 台北週六 07:00;保險排:UTC 週一 23:00 = 台北週二 07:00
# (台北週二那班通常才真的抓到上週五快照)。全 idempotent,重複跑無害。
# 月營收由 fetch_revenue.py 內建「當月 >=11 日才有上月」月守衛,週跑也安全。
on:
  schedule:
    - cron: "0 23 * * 5"
    - cron: "0 23 * * 1"
  workflow_dispatch:        # 允許網頁手動觸發

permissions:
  contents: write

jobs:
  weekly:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - name: Install dependencies
        run: pip install requests pandas pyyaml pyarrow

      - name: Holders (集保,全市場 universe)
        env:
          FINMIND_TOKEN: ${{ secrets.FINMIND_TOKEN }}
        run: python scripts/fetch_holders.py

      - name: Float (流通張數,universe;需 holders 先跑)
        env:
          FINMIND_TOKEN: ${{ secrets.FINMIND_TOKEN }}
        run: python scripts/fetch_float.py

      - name: Revenue (月營收,universe;內建月守衛)
        env:
          FINMIND_TOKEN: ${{ secrets.FINMIND_TOKEN }}
        run: python scripts/fetch_revenue.py

      # 策略資料層:重 per-id(每檔 ~2000 call);--wait-quota 讓單一 job 內等額度跑完
      # (Actions job 上限 6hr,足夠)。放 weekly 而非 nightly 以免撐爆每晚額度。
      - name: Adj price (還原股價,universe)
        env: { FINMIND_TOKEN: "${{ secrets.FINMIND_TOKEN }}" }
        run: python scripts/fetch_stockseries.py --dataset adj --wait-quota
      - name: Short balances (融券/借券餘額,universe)
        env: { FINMIND_TOKEN: "${{ secrets.FINMIND_TOKEN }}" }
        run: python scripts/fetch_stockseries.py --dataset short --wait-quota
      - name: Pledge (借貸擔保品,universe)
        env: { FINMIND_TOKEN: "${{ secrets.FINMIND_TOKEN }}" }
        run: python scripts/fetch_stockseries.py --dataset pledge --wait-quota
      - name: Short suspension (停券/回補日,universe)
        env: { FINMIND_TOKEN: "${{ secrets.FINMIND_TOKEN }}" }
        run: python scripts/fetch_stockseries.py --dataset shortsusp --wait-quota
      - name: CB daily + institutional (per-cb)
        env: { FINMIND_TOKEN: "${{ secrets.FINMIND_TOKEN }}" }
        run: python scripts/fetch_cb.py --tables daily,institutional --wait-quota

      - name: Refresh manifest (latest.json)
        env:
          FINMIND_TOKEN: ${{ secrets.FINMIND_TOKEN }}
        run: python scripts/update.py --manifest-only

      - name: Commit new data
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/
          # 只有真的有變動才 commit(週更常常 no-op,避免空 commit 讓 workflow 紅燈)
          if git diff --staged --quiet; then
            echo "沒有新週更資料,略過 commit"
          else
            git commit -m "auto: weekly data update $(date -u +%Y-%m-%d)"
            git push
          fi
